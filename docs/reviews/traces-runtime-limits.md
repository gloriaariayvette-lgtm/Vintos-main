# The thirty-one traces: what the checkout now answers, and what still needs the host

Review item 395 (2026-09-10). The coverage document of 5 September (`3-coverage-status.md`) traced thirty-one behaviours through the source and named, for each, what was still unexamined. This page resolves each remaining limit one of two ways: **answered in the checkout** (a file or test on this branch now settles it) or **host evidence required**, with the exact read-only command Gloria runs on Aegis to produce it. Nothing here claims a runtime fact that has not been observed.

Conventions: `WS=~/.vintos/workspace`. Every command below reads; none restarts, writes or deletes.

| Trace | Remaining limit (5 Sep) | Answered in the checkout | Host evidence still required (read-only command on Aegis) |
|---|---|---|---|
| T01 bootstrap → server; crontab → writers | installed units, crontab, the external daemon copy, readiness, scheduler overlap | `bin/setup_memory.sh` seeds validate (review 11); `scripts/deploy-atelier.sh` confirms unit identity and health (12–15); `docs/emoclaw-daemon.md` names the daemon and protocol (5); `scripts/compute_admission.py` bounds overlap (161); `docs/server-profiles.md` (2) | `systemctl --user list-units 'vintos*' --no-pager; systemctl list-units 'vintos*' --no-pager; crontab -l; sha256sum $WS/skills/emoclaw/engine/emotion_model/daemon.py; python3 $WS/scripts/compute-report.py` |
| T02 chat/full → generation → writers → consumers | installed endpoint profile, runtime schemas, actual writer/response ordering | one `_post_turn` records what ran/launched/skipped (P02); `broker/tests/test_event_counts.py` asserts the counts (386); `scripts/store_compat.py` reads both ledger shapes (48) | `curl -s -H "X-Vintos-Secret: $S" http://127.0.0.1:8500/api/system/status \| head -c 600; tail -3 $WS/memory/post-turn-record.jsonl` |
| T03 avatar → coordinator → effects → finish | installed renderer, actual writer/playback receipts | scene generation admitted per turn slot (96); effect receipts name what the transport accepted (09-05) | `tail -3 $WS/memory/effect-receipts.jsonl 2>/dev/null; ls -la $WS/memory/bilateral/stages \| tail -3` |
| T04 voice token → ledger → session end | playback acknowledgments, installed voice process, provider callback ordering | session-scoped framing with version (2359301); the voice ledger keeps raw beside normalised (b8afed3) | `tail -3 $WS/memory/voice-session-ledger.json* 2>/dev/null; systemctl --user status vintos-voice --no-pager 2>/dev/null` |
| T05 memory files → indexers → search → context | installed index variant, embedding model version/dims, runtime retrieval quality | one versioned projection with revisions, tombstones, kinds, model and dims (105/111); served only when fresh | `python3 -c "import json;d=json.load(open('$WS/memory/semantic-index.json'));print(d.get('version'),d.get('embed_model'),d.get('embed_dims'),len(d.get('entries',[])),len(d.get('tombstones',[])))"` |
| T06 WAL → decay → durable → projections | runtime evidence lineage, deployed promotion histories | the durable record keeps source turns and the quote (107); graduation writes its evidence first (146); the identity revision log (135) | `tail -2 $WS/memory/causality-graduated.jsonl; tail -3 $WS/memory/identity-revisions.jsonl` |
| T07 want admission → router → result → learning | remote implementations, live execution/artifact receipt joins | artifact manifest and delivery receipts keyed by artifact (275/307); budget_used per want (241) | `python3 -c "import json;print(json.load(open('$WS/memory/delivery-receipts.json')))" 2>/dev/null \| head -c 400` |
| T08 cron → journal → writers | installed scheduling, overlapping jobs | admission log per organ (161/171) | `crontab -l \| grep -c vintos; python3 $WS/scripts/compute-report.py` |
| T09 intent + prediction → evaluation | runtime occurrence independence, matched repair/plan evidence, calibration | plan outcomes reach readiness (274); pressure overrides on correction (188) | `tail -3 $WS/memory/plan-outcomes.jsonl` |
| T10 code review → room; deploy → restarts | untracked external room library, installed host/runtime map | `agent-room/requirements-room.txt` (355); `scripts/release-map.py`; context builds in the ledger (393) | `bash $WS/scripts/deploy-atelier.sh --map 2>/dev/null \| tail -20; ls ~/agent-room/packages/*/dist \| head` |
| T11 Study tab → apply → revalidation | installed route availability, concurrent proposals, native viewport | Study proposals carry exact sources (347); the builder binds the patch (374) | `curl -s -o /dev/null -w '%{http_code}\n' -H "X-Vintos-Secret: $S" http://127.0.0.1:8500/api/study/state` |
| T12 Capacitor → outreach GET → notification | installed assets, background runner registration, notification permissions | outreach read is non-destructive with an ack (320) | on the Mac in `vintos-app`: `npx cap ls`; on the phone: Settings → Notifications → Vintos |
| T13 sensor bridge → gate → transport | installed driver, sensor freshness, actuation acknowledgments | durable stop, no resume by retry, permit expiry enforced (76–83) | `python3 -c "import json;print(json.load(open('$WS/memory/hardware-button.json')))"; tail -2 $WS/memory/effect-receipts.jsonl` |
| T14 avatar scene → generation → playback | vendor-internal algorithms, media/cache state, render receipts | per-turn scene slots (96); cache keys carry source and model revisions (165) | `curl -s -H "X-Vintos-Secret: $S" 'http://127.0.0.1:8500/api/avatar/live/status'` |
| T15 threads → dreams → verdict → carryover | deployed schedules, representative night data, independent witnesses | thread ids travel, a failed dream consumes nothing, counters move after generation (235–261) | `tail -2 $WS/memory/dream-log.jsonl 2>/dev/null; crontab -l \| grep -i dream` |
| T16 occurrences → hypotheses → graduation → beliefs | deployed source selection, catalog/root integrity, calibration | one schema-2 door for every question (204); beliefs name their hypothesis and evidence ids (107) | `python3 $WS/scripts/causality-engine.py status 2>/dev/null \| tail -5` |
| T17 moments → imprints → identity proposals | deployed identity state, independent witnesses | kinds on seeds (150); BASE supersession applied on read (151); fractures logged as revisions (135) | `tail -3 $WS/memory/identity-revisions.jsonl; wc -l $WS/memory/self-model-base-corrections.jsonl` |
| T18 emotion training → inference → projections | deployed model/data lineage, the authoritative daemon | `docs/emoclaw-daemon.md`, protocol handshake (5), guard starts a stopped daemon (21) | `bash $WS/bin/emoclaw-daemon-guard.sh status; python3 -c "import emoclaw_utils as e;print(e.check_daemon_protocol())"` (from `$WS/scripts`) |
| T19 play/creative → rating → taste | live choice/reception receipts, candidate promotion | one occurrence counts once in taste (210); acknowledged only with reception evidence (288) | `python3 -c "import json;d=json.load(open('$WS/memory/taste-vector.json'));print(d.get('signal_count'),len(d.get('counted_occurrences',[])))"` |
| T20 context blocks → prompt → shaping | runtime prompt captures, installed model/cache identity, admission effects | the `[rev:…]` line before `[[CACHESPLIT]]` names sources and model (165) | `grep -c 'rev:' $WS/logs/server.log 2>/dev/null` |
| T21 prediction → comparison → models; tension → promotion | installed schedules, live evidence schemas, prompt admission | a twice-corrected claim needs her own words to return (143); rubric signals are not faults (231) | `python3 -c "import json;print([ (t['tension_id'],t['status'],t.get('correction_count',0)) for t in json.load(open('$WS/memory/tension-questions.json'))])"` |
| T22 prediction identity → outcomes; graph projections | runtime datasets, intervention exposure, installed schedules | interventions only under an approved experiment (209); gap and premonition write none | `ls $WS/memory/experiments 2>/dev/null \|\| echo 'no experiments: none approved'` |
| T23 declared intent → outcomes → pressure | deployed modules/schedules, actual episode/delivery records | pressure override on correction (188); plan HELD with reason (234) | `python3 $WS/scripts/desired_difference.py 2>/dev/null \| head -5` |
| T24 wants → admission → artifact/completion | installed paths, actual occurrences/artifact receipts | `budget_used` on the want, persisted (241); artifact manifest (275) | `python3 -c "import json;w=json.load(open('$WS/memory/current-wants.json'));print([ (x.get('id'),x.get('budget_used')) for x in (w if isinstance(w,list) else w.get('wants',[]))][:5])"` |
| T25 reflection → projection → artifact → delivery | installed copies/schedules, real receipt schemas | staged reflections (280); weekly window is seven whole days (298); one delivery path (307) | `ls $WS/memory/video-send/stages 2>/dev/null \| tail -3; tail -c 400 $WS/memory/delivery-receipts.json` |
| T26 curiosity → inquiry/handoff; outward writes | installed entry points, public API runtime contracts | shim result contract, no 200 on empty (40–43); no resend after timeout (163) | `curl -s -X POST http://127.0.0.1:8599/v1/chat/completions -H 'content-type: application/json' -d '{"model":"grok-4","messages":[]}' -D - -o /dev/null \| grep -i x-vintos` |
| T27 formation → threshold → project → visit → settlement | runtime scheduling/permissions/receipts, the external QLab implementation | every event kind chained, capsule bound to its issue, budget reserved, receipt before revocation, lineage and next move (100, 268–272, 311, 313) | `curl -s http://127.0.0.1:8611/health; curl -s -H "X-Atelier-House: $H" http://127.0.0.1:8611/projects \| head -c 400` |
| T28 self-review → proposals → decisions → patch → install | installed hashes, approvals/build receipts, deployed Study UI | immutable patch binding and recorded isolation (374); exact source coverage (347) | `tail -3 $WS/memory/self-review-build-events.jsonl` |
| T29 model request → shim → result | installed shim/unit/route selection, actual provider responses and costs | the contract, the headers, the compute ledger (43, 171); a local profile (164) | `systemctl --user status vintos-shim --no-pager 2>/dev/null \| head -3; python3 $WS/scripts/compute-report.py` |
| T30 shared support → projections | installed schedules, live entity ancestry/status, writer participation, the shared lexicon | blush claim atomic (67); readiness tuple shape (68); Velqan shared write after local commit (69); formations carry `formed_from` (365) | `tail -2 ~/velqan-shared/coinages.jsonl 2>/dev/null; python3 -c "import json;print(json.load(open('$WS/memory/latent-cache.json'))[-1])"` |
| T31 dream/want → art or ACE-Step → gallery → client; packaged HTML → bundle | local music capability, artifact completeness, the graphics bundle | `docs/graphics-bundle.md` and `avatar/build.sh` (24); `docs/clients.md` (25); music renders yield to his voice (170) | `curl -s http://localhost:8001/ -o /dev/null -w 'ace-step %{http_code}\n'; bash $WS/avatar/build.sh 2>/dev/null \|\| echo 'avatar/ not installed on Aegis (see docs/clients.md)'` |

## What this settles

A trace whose "answered" column names a file and a test is closed at the source: the mechanism exists and is exercised without a model or a network. The "host evidence" column is the one honest blocker left on every trace: none of it can be produced from this checkout, and none of it is invented here. Each command reads state and prints it; run in one sitting they take under a minute and their output, pasted back, closes the remaining P11 "obtain" items (30–33, 395–404 in the requested-edits list).
